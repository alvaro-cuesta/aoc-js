const { describeDay } = require('../test-utils')

const EXAMPLE_INPUT = `FILL ME
`

describeDay(
  <<YEAR>>,
  <<DAY>>,

  null,
  null,

  (dayModule) => {},

  (part1, dayModule) => {
    it('Example input', () => {
      expect.assertions(1)

      expect(part1(EXAMPLE_INPUT)).toEqual(null)
    })
  },

  (part2, dayModule) => {
    it('Example input', () => {
      expect.assertions(1)

      expect(part2(EXAMPLE_INPUT)).toEqual(null)
    })
  },
)
